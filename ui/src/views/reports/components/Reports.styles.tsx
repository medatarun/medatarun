import { makeStyles } from "@fluentui/react-components";

export const useCompactDropdownStyles = makeStyles({
  compactDropdown: {
    width: "fit-content",
    minWidth: "max-content",
    maxWidth: "100%",
    justifySelf: "start",
  },
});
